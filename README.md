# Component Variation test

1. run `sh scripts/init.sh`
2. run `mkdir "resources/[assets]"`
3. put asset resources on `resources/[assets]` directory
4. run server, run client, enter `/main` in game client chat
